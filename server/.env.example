# Copy this file to .env and adjust as needed.
# Local Postgres started via `docker compose up -d` (see docker-compose.yml)
DATABASE_URL="postgresql://aux:aux@localhost:5432/aux?schema=public"
PORT=3000
